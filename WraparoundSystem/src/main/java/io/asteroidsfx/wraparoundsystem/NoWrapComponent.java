package io.asteroidsfx.wraparoundsystem;

import io.asteroidsfx.common.Component;

/**
 * Marker component to indicate that an entity should not use wraparound behavior.
 * Entities with this component will not wrap around the screen edges.
 */
public class NoWrapComponent extends Component {
    // This is a marker component - it has no data, just its presence matters
}

